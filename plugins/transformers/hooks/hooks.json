{
  "description": "Transformers plugin hooks — intentionally empty. Security checks are handled by core-principles loaded into every agent. Plugin hooks fire globally which is not desired.",
  "hooks": {}
}
